version: "3.7"
services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
    
    ports: 
      - 5432:5432

  #backend:
  #  build: ./backend
  #  restart: always
  #  ports: 8080:8080
